这是一个非常棒的升级！使用 **Supabase** (一个开源的 Firebase 替代品，免费且功能强大) 可以完美实现你的需求：
1.  **无需 GitHub 操作**：你只需要访问一个网页版的后台。
2.  **即时更新**：上传图片后，主页立刻就能看到。
3.  **作品管理**：可以添加标题、描述，甚至以后还能加价格、标签等。

### 实施计划

我将把你的网站改造成一个“动态网站”，直接从 Supabase 读取数据。

#### 1. 代码改造
-   **创建 `config.js`**: 用于存放你的 Supabase 密钥。
-   **创建 `admin.html`**: 一个简单的后台管理页面，用于上传图片和写描述。
-   **修改 `index.html`**: 不再写死图片，而是用 JavaScript 从 Supabase 拉取图片列表。
-   **修改 `script.js`**: 添加连接 Supabase 的逻辑。

#### 2. 你需要配合的操作 (在 Supabase 官网)
我无法帮你注册账号，所以需要你稍后按照我的指引去 Supabase：
1.  创建一个新项目。
2.  创建一个表格 `artworks` (存作品信息)。
3.  创建一个存储桶 `portfolio` (存图片文件)。
4.  把 API Key 填入 `config.js`。

我现在开始编写代码，稍后会给你详细的 Supabase 设置指南。